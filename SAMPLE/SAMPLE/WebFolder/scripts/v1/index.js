(function(w, $, a, m, q){		function filterEvent(e){		e.stopPropagation();		e.preventDefault();			return false;	}		//disable text selection on page		$('body').on('selectstart', filterEvent).on('drop', filterEvent).on('dragover', filterEvent).css({		'-webkit-user-select':'none',		'-moz-user-select':'none',		'-ms-user-select':'none',		'-o-user-select':'none',		'user-select':'none'	});		//jQuery references to page elements		var qrcodeData$ = $('#qrcode-data');	var qrcodeImage$ = $('#qrcode-image');													a.set({'delay':1500});    m.lang(navigator.language);	    		w.onAfterInit = function(){				//just cosmetic, no impact on performance		window.history.replaceState('Object', 'Title', '/');				qrcodeData$.on('change', function(e){			qr.encodeAsync({				onSuccess:function(e){					if(e){						qrcodeImage$.removeClass('hidden').attr('src', e);					}else{						qrcodeImage$.addClass('hidden');								}				},				onError:function(){a.error('an error has occured while calling RPC.')},					params:[qrcodeData$.val(), 10, 'L', 'auto']					});		});	};		})(WAF, jQuery, alertify, moment);